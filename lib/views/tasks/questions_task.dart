import 'package:flutter/material.dart';

class QuestionsTaskScreen extends StatelessWidget {
  const QuestionsTaskScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Questions'), // Add a title to the app bar
      ),
      body: Center(child: Text("To be implemented...")),
    );
  }
}
